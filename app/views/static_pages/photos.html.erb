<% provide(:title, "PHOTOS") %>
<section class="center border main">
	<h3>PHOTOS</h3>
	<% @photos.each do |photo| %>
	<section class="gallery">
		<section class="photo">
			<%= link_to image_tag(photo, :height => 200), photo %>
		</section>
	</section>
	<% end %>
</section>