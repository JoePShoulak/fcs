<% provide(:title, "MUSIC") %>
<section class="center border">
	<h3>MUSIC</h3>
	</br>
	<% @albums.each do |album| %>
		<%= link_to( image_tag(album[1], class: "album"), album[0]) %>
	<% end %>
	<a>Getting Good at Starting Over</a> (Coming soon!)
</section>