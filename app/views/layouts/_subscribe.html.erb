<section id="subscribe" class="border">
	<span id="subcribe-title">Get updates from Preston!</span>
	<%= form_for :user, url: users_path do |f| %>
	    <%= f.text_field :email, class: "field" %>
	    <%= f.submit "Subscribe!", class: "btn btn-primary", id: "button" %>
	<% end %>
</section>